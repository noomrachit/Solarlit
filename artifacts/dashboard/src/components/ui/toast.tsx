// Sonner handles the actual toasts; these stubs satisfy component imports.
export const toast = () => {};
export type ToastProps = Record<string, unknown>;
export type ToastActionElement = React.ReactElement;
